export interface EventDisplay {
  eventId: number;
  dateCreated: Date;
  dateTimeOfEvent: Date;
  lengthOfEvent: Date;
  totalCost: number;
  eventStatus: boolean;
  eventAddress: string;
  postcode: string;
}
